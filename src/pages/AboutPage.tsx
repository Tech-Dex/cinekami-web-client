import { Stack, Text, Title } from '@mantine/core';

export default function AboutPage() {
  return (
    <Stack>
      <Title order={2}>About</Title>
      <Text>This is a starter using React Router v7, TanStack Query, Mantine, and Context API.</Text>
    </Stack>
  );
}

